name: build-env
channels:
  - conda-forge
dependencies:
  - python
  - jupyter_server
  - jupyterlab
  - pip:
      - jupyterlite-core >=0.2.0a0,<0.3.0
      - jupyterlite-xeus-python >=1.0.0a0,<2.0.0
